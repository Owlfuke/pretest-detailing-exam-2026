{
  "name": "rc-detailing-exam-2026",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
